[plugin]

name="Editor Bridge"
description="File-mailbox bridge between CLI tools and the Godot editor (commands from disk, no restarts; game ops proxied to the in-game DebugBridge on 5555). Replaces editor_run_server."
author="OpenAgent"
version="2.0.0"
script="editor_bridge.gd"
